const getters = require('./getters');
const mutations = require('./mutations');
const actions = require('./actions');

const state = {
  message: Object,
};

export default {
  state,
  getters,
  mutations,
  actions,
};
